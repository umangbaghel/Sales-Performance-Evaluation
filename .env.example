# Copy this file to .env and fill in your values
# The app reads this automatically — no need to edit any PHP files

DB_HOST=localhost
DB_NAME=sales
DB_USER=root
DB_PASSWORD=
DB_PORT=3306

APP_ENV=development
APP_DEBUG=true
